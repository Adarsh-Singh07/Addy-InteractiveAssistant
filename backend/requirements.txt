# ── Phase 1 core dependencies ─────────────────────────────────────────────────

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Settings
pydantic>=2.12.5
pydantic-settings>=2.7.0

# HTTP client (for Deepgram TTS REST)
httpx>=0.28.1

# WebSocket support (for Deepgram STT)
websockets==13.1

# Logging
structlog==24.4.0
colorama==0.4.6   # Windows color support in structlog dev renderer

# System metrics
psutil==6.0.0

# ── LLM providers ──────────────────────────────────────────────────────────────
google-genai>=2.17.0
groq>=0.16.0

# ── STT / TTS ──────────────────────────────────────────────────────────────────
deepgram-sdk==3.7.0

# ── Testing ────────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
httpx>=0.28.1     # for TestClient

# ── Phase 3 (not loaded in Phase 1 — in Docker only) ─────────────────────────
# sentence-transformers==3.0.1
# qdrant-client==1.11.0
# PyMuPDF==1.24.10
# python-docx==1.1.2
